{
  "name": "push-admin",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsgo --noEmit && vue-tsc -b && vite build",
    "typecheck": "tsgo --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.38"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260421.2",
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.0.16",
    "vue-tsc": "^3.3.5"
  }
}
